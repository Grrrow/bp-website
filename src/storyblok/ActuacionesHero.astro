---
import { storyblokEditable } from '@storyblok/astro'

const { blok } = Astro.props
---

<header {...storyblokEditable(blok)} class="actuaciones-hero">
  <div class="container">
    <div class="hero-content">
      {blok.label && <span class="archive-label">{blok.label}</span>}
      <h1 class="hero-headline">{blok.headline}</h1>
      <div class="accent-line"></div>
    </div>
  </div>
</header>

<style>
  .actuaciones-hero {
    padding-top: 15rem; /* Space for the sticky header */
    padding-bottom: 4rem;
    background-color: #000000;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1.5rem;
  }

  .archive-label {
    font-family: var(--font-body);
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.25em;
    color: #94a3b8;
    font-weight: 500;
  }

  .hero-headline {
    font-family: var(--font-display);
    font-size: clamp(3.5rem, 9vw, 6.5rem); /* Balanced size */
    font-weight: 300;
    color: #ffffff;
    line-height: 1.1;
    margin: 0;
    letter-spacing: -0.01em;
  }

  .accent-line {
    width: 100px;
    height: 1px;
    background-color: var(--color-primary); /* Use primary color as per design accent */
    margin-top: 1rem;
    opacity: 0.4;
  }

  @media (max-width: 1024px) {
    .actuaciones-hero {
      padding-top: 12rem;
      padding-bottom: 3rem;
    }
  }

  @media (max-width: 768px) {
    .hero-headline {
      font-size: 3rem;
    }
    .actuaciones-hero {
       padding-top: 4rem;
       padding-bottom: 2rem;
    }
  }
</style>
